{
 "items": [
  {
   "sys": { "id": "1" },
   "fields": {
    "title": "executive parlour",
    "price": 200,
    "image": { "fields": { "file": { "url": "/img/hm2.jpg"}}}
   }
  },
  {
   "sys": { "id": "2" },
   "fields": {
    "title": "executive parlour",
    "price": 200,
    "image": { "fields": { "file": { "url": "/img/hm2.jpg"}}}
   }
  }
 ]
}

/**class Products{
 async getProducts() {
  try {
   let result = await fetch("products.json");
   let data = await result.json();
   let products = data.items;
   products = products.map(item => {
    const { title, price } = item.fields;
    const { id } = item.sys
    const image = item.fields.image.fields.file.url;
    return { title, price, id, image }
   });
   return products;
  } catch (error) {
   console.log(error);
  }
 }
}*/
// display products
